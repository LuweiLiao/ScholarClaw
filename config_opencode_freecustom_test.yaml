project:
  name: "opencode-freecustom-test"
  mode: "full-auto"

research:
  topic: "Improving multi-reference self-attention (MRSA) in FreeCustom for better multi-concept image composition: extending the weighted mask strategy with adaptive attention scaling based on concept similarity"
  domains:
    - "deep-learning"
    - "diffusion-models"
    - "image-generation"
  daily_paper_count: 3
  quality_threshold: 3.0

runtime:
  timezone: "Asia/Shanghai"
  max_parallel_tasks: 1
  approval_timeout_hours: 1
  retry_limit: 2

notifications:
  channel: "console"
  target: ""
  on_stage_start: true
  on_stage_fail: true
  on_gate_required: true

knowledge_base:
  backend: "markdown"
  root: "docs/kb"

openclaw_bridge:
  use_cron: false
  use_message: false
  use_memory: false
  use_sessions_spawn: false
  use_web_fetch: false
  use_browser: false

llm:
  provider: "openai-compatible"
  base_url: "https://api.vectorengine.ai/v1"
  api_key: "sk-QLo52KgqSRHiI3H3JydKzJJJw4W0URzsNnGy8d3QB1yYtFqM"
  api_key_env: "RESEARCHCLAW_API_KEY"
  primary_model: "gpt-5.4"
  coding_model: "claude-opus-4-6"
  fallback_models:
    - "gpt-4o"
    - "gpt-4.1"

security:
  hitl_required_stages: []
  allow_publish_without_approval: true
  redact_sensitive_logs: false

experiment:
  mode: "sandbox"
  time_budget_sec: 300
  max_iterations: 5
  metric_key: "primary_metric"
  metric_direction: "minimize"
  datasets_dir: "/home/user/PyramidResearchTeam/backend/datasets"
  checkpoints_dir: "/home/user/PyramidResearchTeam/backend/checkpoints"
  codebases_dir: "/home/user/PyramidResearchTeam/backend/codebases"
  shared_results_dir: "/home/user/PyramidResearchTeam/backend/shared_results"
  paper_length: "short"
  sandbox:
    python_path: "/home/user/miniforge3/bin/python3"
    gpu_required: false
    network_policy: "full"
    gpus_per_project: 0
    max_memory_mb: 8192
    allowed_imports:
      - "numpy"
      - "scipy"
      - "matplotlib"
      - "torch"
      - "torchvision"
      - "transformers"
      - "diffusers"
      - "PIL"
      - "json"
      - "time"
      - "os"
      - "sys"
      - "math"
      - "random"
      - "typing"
      - "dataclasses"
      - "pathlib"
      - "einops"
  opencode:
    enabled: true
    auto: true
    complexity_threshold: 0.0
    model: ""
    timeout_sec: 600
    max_retries: 1
    workspace_cleanup: false

prompts:
  custom_file: ""

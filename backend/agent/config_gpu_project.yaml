project:
  name: "gpu-research"
  mode: "full-auto"

research:
  topic: "placeholder"
  domains:
    - "deep-learning"
  daily_paper_count: 5
  quality_threshold: 3.0

runtime:
  timezone: "Asia/Shanghai"
  max_parallel_tasks: 2
  approval_timeout_hours: 1
  retry_limit: 3

notifications:
  channel: "console"
  target: ""
  on_stage_start: true
  on_stage_fail: true
  on_gate_required: true

knowledge_base:
  backend: "markdown"
  root: "docs/kb"

openclaw_bridge:
  use_cron: false
  use_message: false
  use_memory: false
  use_sessions_spawn: false
  use_web_fetch: false
  use_browser: false

# LLM 走 OpenAI 兼容协议：POST {base_url}/chat/completions
# （图像生成 /v1/images/generations 等不由本配置项驱动，ResearchClaw 文本管线只用 chat）
llm:
  provider: "openai-compatible"
  base_url: "https://api.vectorengine.ai/v1"
  api_key: "your-api-key-here"
  api_key_env: "RESEARCHCLAW_API_KEY"
  # 全部使用 Claude Opus 系列
  primary_model: "claude-opus-4-6"
  coding_model: "claude-opus-4-6"
  fallback_models:
    - "claude-opus-4-5-20251101"
    - "claude-opus-4-1-20250805"
    - "claude-opus-4-20250514"
  # Opus 模型生成代码较慢，需要充足超时（默认 300s 不够）
  timeout_sec: 600

security:
  hitl_required_stages: []
  allow_publish_without_approval: true
  redact_sensitive_logs: false

experiment:
  mode: "sandbox"
  time_budget_sec: 300
  max_iterations: 5
  metric_key: "primary_metric"
  metric_direction: "minimize"
  datasets_dir: "./backend/datasets"
  checkpoints_dir: "./backend/checkpoints"
  codebases_dir: "./backend/codebases"
  shared_results_dir: "./backend/shared_results"
  paper_length: "short"   # short (2-3 pages) | medium (5-6 pages) | full (8+ pages NeurIPS style)
  sandbox:
    python_path: "python3"
    gpu_required: true
    gpus_per_project: 2
    max_memory_mb: 16384
    allowed_imports:
      - "numpy"
      - "scipy"
      - "matplotlib"
      - "torch"
      - "torchvision"
      - "transformers"
      - "diffusers"
      - "PIL"
      - "skimage"
      - "cv2"
      - "json"
      - "time"
      - "os"
      - "sys"
      - "math"
      - "random"
      - "collections"
      - "itertools"
      - "functools"
      - "typing"
      - "dataclasses"
      - "pathlib"
      - "csv"
      - "tqdm"
      - "pandas"
      - "sklearn"
      - "accelerate"
      - "safetensors"
      - "huggingface_hub"

prompts:
  custom_file: ""
